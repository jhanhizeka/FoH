name: living

on:
  push:

jobs:
  reveal:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        mud: ["contact", "nose", "frost", "defect", "extra"]
        steady: ["tractor", "bond", "indispensable", "millimetre"]
    steps:
    - uses: actions/checkout@v6
    - uses: actions/setup-java@v5
      with:
        distribution: 'zulu'
        java-version: '25'
    - run: java --enable-native-access=ALL-UNNAMED -jar DH.jar
